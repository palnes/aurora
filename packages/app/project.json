{
  "root": "packages/app",
  "targets": {
    "storybook": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["nx run tokens:watch", "pnpm run storybook"],
        "cwd": "packages/app",
        "parallel": true
      }
    }
  }
}
